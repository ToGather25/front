# 헤더 · 메인 · 푸터 GAP 분석 및 개선 계획

> 스펙 기준으로 현재 구현 상태를 비교한 문서입니다.
> ✅ 구현됨 · ⚠️ 부분 구현 · ❌ 미구현

---

## 1. 헤더 (`RootLayout.jsx` — `DesktopHeader`)

### 1-1. 메뉴 클릭 시 이동 경로

| 메뉴 | 스펙 (첫 번째 진입 페이지) | 현재 (`children[0]`) | 상태 | 수정 파일 |
|---|---|---|---|---|
| 교회소개 | `/교회소개?tab=인사말` | `/교회소개?tab=인사말` | ✅ | — |
| 예배·방송 | `/말씀/설교` | `/말씀/설교` | ✅ | — |
| 주일학교 | `/주일학교/초등부` | `/주일학교/유치부` | ❌ | `church.config.js` |
| 전도선교 | `/전도선교/전도회` | `/전도선교/전도회` | ✅ | — |
| 양육훈련 | `/양육훈련/제자훈련` | `/양육훈련/구역` | ❌ | `church.config.js` |
| 교회소식 | `/주보` | `/공지사항` | ❌ | `church.config.js` |

**수정 내용 (`church.config.js`):**
- 주일학교 `children` 순서 변경: 초등부를 첫 번째로
- 양육훈련 `children` 순서 변경: 제자훈련을 첫 번째로
- 교회소식 `children` 순서 변경: 스마트 주보를 첫 번째로

---

### 1-2. 비로그인 시 교적부 처리

| 항목 | 스펙 | 현재 | 상태 |
|---|---|---|---|
| 교적부 버튼 노출 | 비로그인 시에도 **보이되 회색 비활성화** | 비로그인 시 아예 없음 | ❌ |
| 교적부 클릭 시 | "로그인이 필요한 서비스입니다" 팝업 | — | ❌ |
| 로그인 시 교적부 | 교적부 페이지로 이동 | ✅ 구현됨 | ✅ |
| 로그아웃 | 로그아웃 후 비로그인 헤더로 전환 | ✅ 구현됨 | ✅ |
| 회원가입 / 로그인 버튼 | 각 페이지로 이동 | ✅ 구현됨 | ✅ |

**수정 내용 (`RootLayout.jsx` — `DesktopHeader`):**
- 비로그인 시에도 교적부 버튼 노출, `text-grey-4 cursor-default` 스타일로 비활성화
- 클릭 시 "로그인이 필요한 서비스입니다" 팝업(모달) 노출

---

## 2. 메인 배너 (`MainBanner.jsx`)

| 항목 | 스펙 | 현재 | 상태 |
|---|---|---|---|
| 이번 주 예배 보기 → /말씀/방송 | ✅ | ✅ | ✅ |
| 라이브 중 → 라이브 페이지 | /말씀/방송이 live 감지 | ✅ | ✅ |
| 예배 안내 → /교회소개?tab=예배 안내 | ✅ | ✅ | ✅ |

> 메인 배너는 GAP 없음.

---

## 3. 검색 섹션 (`SearchSection.jsx`)

| 항목 | 스펙 | 현재 | 상태 |
|---|---|---|---|
| 검색창 클릭 → 검색 모달/상단 이동 | ✅ | SearchContext 모달 열림 | ✅ |
| 검색 결과 페이지 (`/검색`) | 별도 페이지로 이동 | 모달 내에서만 처리, 결과 페이지 없음 | ❌ |
| 자주 찾는 버튼 → 해당 페이지로 직접 이동 | 각 버튼이 특정 페이지로 이동 | 클릭 시 `setOpen(true)`만 실행 (모달만 열림) | ❌ |

**수정 내용 (`SearchSection.jsx`):**
- 자주 찾는 버튼(`SUGGESTIONS`)에 각각 `to` 경로 매핑하여 `Link`로 변경
  - 주일 예배 → `/말씀/방송`
  - 성경 타자 → `/말씀/필사`
  - 오시는 길 → `/교회소개?tab=오시는 길`
  - 구역모임 → `/양육훈련/구역`
  - 헌금 안내 → `/주보?tab=예물`
- 검색 결과 페이지는 백엔드 연동 시 별도 구현 (현재 모달로 유지)

---

## 4. 바로가기 메뉴 (`SubMenu.jsx`)

| 항목 | 스펙 | 현재 | 상태 |
|---|---|---|---|
| 교회소개 → /교회소개 | ✅ | ✅ | ✅ |
| 행사·소식 → /교회행사 | ✅ | ✅ | ✅ |
| 스마트 주보 → /주보 | ✅ | ✅ | ✅ |
| 성경 타자 → 성경 타자 페이지 | `/말씀/필사` | `/양육훈련` | ❌ |

**수정 내용 (`SubMenu.jsx`):**
- 성경 타자 `to`: `/양육훈련` → `/말씀/필사`

---

## 5. 이번 주 말씀 섹션 (`WorshipSection.jsx`)

### 5-1. CTA 버튼

| 항목 | 스펙 | 현재 | 상태 |
|---|---|---|---|
| 기본 상태: "다시 보기" → 최근 영상 페이지 이동 | `/말씀/방송` | 버튼에 링크 없음 | ❌ |
| 라이브 시: "실시간 방송 보기" → 라이브 페이지 | `/말씀/방송` | 항상 "다시 보기" 고정 | ❌ |
| 라이브 여부: 관리자 설정값 기준 | — | YouTube API로 감지 중 | ⚠️ |

### 5-2. 영상 썸네일

| 항목 | 스펙 | 현재 | 상태 |
|---|---|---|---|
| 재생 버튼 클릭 → 예배 영상 페이지로 이동 | `/말씀/방송` | 인라인 iframe 재생 | ❌ |
| 라이브 중 → 라이브 플레이어로 연결 | `/말씀/방송` | 인라인 재생 | ❌ |
| LIVE 뱃지 노출 | ✅ | ✅ | ✅ |
| 설교 제목, 설교자, 예배 시간 표시 | ✅ | ✅ (하드코딩) | ⚠️ |

**수정 내용 (`WorshipSection.jsx`):**
- "다시 보기" 버튼: `<button>` → `<Link to="/말씀/방송">`
- `isLive`가 true일 때 버튼 텍스트 "실시간 방송 보기"로 변경
- `VideoThumb` 재생 버튼 클릭 시 인라인 재생 대신 `/말씀/방송`으로 `navigate`

---

## 6. 예배 안내 카드 (`ServiceBand.jsx`)

| 항목 | 스펙 | 현재 | 상태 |
|---|---|---|---|
| 예배명, 시간, 장소 표시 | ✅ | ✅ | ✅ |
| 카드 클릭 → 예배 상세 안내 페이지 | `/교회소개?tab=예배 안내` | 클릭 기능 없음 | ❌ |
| 전체 안내 버튼 → /교회소개?tab=예배 안내 | ✅ | ✅ | ✅ |

**수정 내용 (`ServiceBand.jsx`):**
- 각 예배 카드를 `<Link to="/교회소개?tab=예배 안내">`로 감싸기 + hover 스타일 추가

---

## 7. 공지·소식 / 다가오는 일정 (`NoticeSection.jsx`)

### 7-1. 공지사항

| 항목 | 스펙 | 현재 | 상태 |
|---|---|---|---|
| 탭 필터 (전체/공지/행사/소식) | ✅ | ✅ | ✅ |
| 최신 5개 노출 | ✅ | ✅ | ✅ |
| 고정 게시글 최상단 + 뱃지 + 볼드 | ✅ | ✅ (featured 필드) | ✅ |
| 분류태그, 제목, 등록일 표시 | ✅ | ✅ | ✅ |
| 제목 한 줄 말줄임 | `truncate` | ✅ | ✅ |
| 행 클릭 → 상세 페이지 | ✅ | ✅ | ✅ |
| 전체보기 → /공지사항 | ✅ | ✅ | ✅ |
| 공지 없을 때 Empty state | ✅ | ✅ | ✅ |

### 7-2. 다가오는 일정

| 항목 | 스펙 | 현재 | 상태 |
|---|---|---|---|
| 현재 날짜 기준 미래 일정 최대 4개 | ✅ | 하드코딩 고정 (날짜 필터 없음) | ❌ |
| 날짜(MM.DD), 요일, 일정명, 시간·장소 | ✅ | ✅ | ✅ |
| 일정명 Bold | ✅ | ✅ | ✅ |
| 일정 제목 한 줄 말줄임 | `truncate` | 없음 | ❌ |
| 카드 클릭 → 상세 모달/페이지 `/교회행사/:id` | ✅ | 클릭 기능 없음 | ❌ |
| 일정 없을 때 Empty state | "등록된 정보가 없습니다." | 없음 | ❌ |
| 캘린더로 보기 → /교회행사 | ✅ | ✅ | ✅ |

### 7-3. 반응형

| 항목 | 스펙 | 현재 | 상태 |
|---|---|---|---|
| 모바일: 좌우 2단 → 상하 1단 (공지 위, 일정 아래) | ✅ | `grid-cols` 고정, 반응형 없음 | ❌ |

**수정 내용 (`NoticeSection.jsx`):**
- `UPCOMING_EVENTS`에 실제 날짜 필드 추가, 현재 날짜 이후 항목만 최대 4개 필터링
- 일정 카드: `cursor-pointer` + `onClick` → `/교회행사/:id` 이동
- 일정 제목에 `truncate` 추가
- 일정 Empty state 추가: "등록된 정보가 없습니다."
- 반응형: `grid grid-cols-1 lg:grid-cols-[1fr_460px]`로 변경

---

## 8. 찾아오시는 길 (`DirectionsSection.jsx`)

| 항목 | 스펙 | 현재 | 상태 |
|---|---|---|---|
| 지도 API 연동 + 마커 | ✅ | KakaoMap 컴포넌트 있음 | ✅ |
| 마커 클릭/기본 로드 시 주소 말풍선 | ✅ | KakaoMap 구현 여부 확인 필요 | ⚠️ |
| 일반 지도 / 스카이뷰 전환 탭 | ✅ | 없음 | ❌ |
| 확대/축소 인터랙션 바 | 카카오맵 기본 제공 | `draggable={false}` 설정으로 컨트롤 숨김 가능성 | ⚠️ |
| 지도 API 오류 처리 문구 | "지도를 불러올 수 없습니다." | 없음 | ❌ |
| 주소, TEL·FAX, 대중교통 표시 | ✅ | ✅ | ✅ |
| 주차/셔틀/문의 버튼 (네이비 와이드) | ✅ | ✅ | ✅ |
| 버튼 hover 시 밝아지는 효과 | opacity 조절 | `hover:bg-blue-8` (어두워짐) | ❌ |
| 반응형: 좌우 → 상하 (지도 위, 정보 아래) | ✅ | `grid` 고정, 반응형 없음 | ❌ |
| 지도 모바일 스크롤 잠금 | ✅ | 없음 | ❌ |

**수정 내용 (`DirectionsSection.jsx` + `KakaoMap.jsx`):**
- 지도 위 일반/스카이뷰 탭 버튼 추가 → `mapTypeId` 전환
- 버튼 hover 효과: `hover:bg-blue-8` → `hover:brightness-110` 또는 `hover:bg-blue-7/90`
- 지도 오류 시 fallback UI 추가
- 반응형: `grid-cols-1 lg:grid-cols-[1fr_540px]`
- 모바일 지도 스크롤 잠금: 터치 이벤트 처리 또는 카카오맵 `draggable: false`

---

## 9. 푸터 (`RootLayout.jsx` — `DesktopFooter`)

| 항목 | 스펙 | 현재 | 상태 |
|---|---|---|---|
| 로고 | ✅ | ✅ | ✅ |
| 개인정보취급방침 볼드 강조 | ✅ | 동일 스타일 (볼드 없음) | ❌ |
| 이용약관 링크 | ✅ | ✅ | ✅ |
| 주소, TEL·FAX, 이메일 | ✅ | ✅ | ✅ |
| 텍스트 복사 가능 (드래그) | ✅ | ✅ (기본 텍스트) | ✅ |
| SNS 아이콘 (유튜브, 인스타, 페이스북) | ✅ | ✅ | ✅ |
| SNS 링크 `target="_blank"` | ✅ | 없음 | ❌ |
| SNS hover 효과 | ✅ | ✅ | ✅ |
| 모바일 푸터 | 세로 1단 (로고→약관→정보→SNS) | `hidden md:block` (모바일 푸터 없음) | ❌ |

**수정 내용 (`RootLayout.jsx` — `DesktopFooter` 개편):**
- 개인정보취급방침 `font-bold` 추가
- SNS 링크에 `target="_blank" rel="noopener noreferrer"` 추가
- 모바일 푸터 추가 (별도 `MobileFooter` 컴포넌트):
  - 로고 → 약관 → 기본정보 → SNS 세로 배치, `text-center`
  - `md:hidden`으로 모바일에서만 노출

---

## 작업 목록 (우선순위 순)

### P1 — 링크·경로만 수정 (간단)
- [ ] `church.config.js` — 주일학교 첫 진입: 유치부 → **초등부**
- [ ] `church.config.js` — 양육훈련 첫 진입: 구역 → **제자훈련**
- [ ] `church.config.js` — 교회소식 첫 진입: 공지사항 → **스마트 주보(/주보)**
- [ ] `SubMenu.jsx` — 성경 타자 링크: `/양육훈련` → `/말씀/필사`
- [ ] `SearchSection.jsx` — 자주 찾는 버튼 `Link`로 교체 + 경로 매핑
- [ ] `ServiceBand.jsx` — 예배 카드 `Link`로 감싸기
- [ ] `WorshipSection.jsx` — "다시 보기" 버튼 `Link` 연결, 라이브 시 텍스트 변경
- [ ] `WorshipSection.jsx` — 재생 버튼 클릭 → `/말씀/방송` navigate
- [ ] `RootLayout.jsx` — SNS 링크 `target="_blank"` 추가
- [ ] `RootLayout.jsx` — 개인정보취급방침 `font-bold`

### P2 — UI 컴포넌트 추가/수정 (중간)
- [ ] `RootLayout.jsx` — 비로그인 시 교적부 버튼 회색 노출 + 클릭 시 팝업
- [ ] `NoticeSection.jsx` — 일정 날짜 필터 (현재 날짜 이후 4개)
- [ ] `NoticeSection.jsx` — 일정 카드 클릭 → `/교회행사/:id`
- [ ] `NoticeSection.jsx` — 일정 `truncate`, Empty state
- [ ] `NoticeSection.jsx` — 반응형 그리드 (`lg:grid-cols-[1fr_460px]`)
- [ ] `DirectionsSection.jsx` — 일반/스카이뷰 탭 추가
- [ ] `DirectionsSection.jsx` — 버튼 hover 효과 수정
- [ ] `DirectionsSection.jsx` — 지도 오류 fallback UI
- [ ] `DirectionsSection.jsx` — 반응형 그리드
- [ ] `RootLayout.jsx` — 모바일 푸터(`MobileFooter`) 추가

### P3 — 백엔드 연동 시 (나중에)
- [ ] `WorshipSection.jsx` — 설교 정보 API 연동 (현재 하드코딩)
- [ ] `NoticeSection.jsx` — 일정 데이터 API 연동
- [ ] 검색 결과 페이지 (`/검색`) 구현
